#ifndef TIPOS_H
#define TIPOS_H
#include "cuadruplas.h"

/* -------------------------Declaracion de Funciones -------------------------- */

char* ampliar(char *dir, int t1, int t2, code *codigo);         //Pendiente
char* reducir(char *dir, int t1, int t2, code *codigo);         //Pendiente
int max(int t1, int t2);                                        //Pendiente
int min(int t1, int t2);                                        //Pendiente

#endif
