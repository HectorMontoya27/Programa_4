#ifndef TIPOS_H
#define TIPOS_H

/* -------------------------Declaracion de Funciones -------------------------- */

char* ampliar(char *dir, int t1, int t2, code *codigo, T_Tipos *tabla);         //Pendiente
char* reducir(char *dir, int t1, int t2, code *codigo, T_Tipos *tabla);         //Pendiente
int max(int t1, int t2, T_Tipos *tabla);                                        //Pendiente
int min(int t1, int t2, T_Tipos *tabla);                                        //Pendiente

#endif
